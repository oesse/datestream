# Session MS

Synced playback control for Partners. Use only `POST` verb for mutations with RPC like route names.
```
POST /new-session
POST /join-session
POST /request-play
POST /request-pause
POST /request-reset
```
